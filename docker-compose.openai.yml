services:
  mcp:
    environment:
      - PROVIDER=OPENAI
      - OPENAI_MODEL=gpt-4o-mini
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      # - OPENAI_BASE_URL=https://api.openai.com/v1  # if using a proxy, set here