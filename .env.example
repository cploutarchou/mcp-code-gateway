# Port the gateway listens on
PORT=3333

# Provider: OLLAMA or OPENAI
PROVIDER=OLLAMA

# If using Ollama (local)
MODEL_BASE_URL=http://ollama:11434
MODEL_NAME=llama3.1:8b

# If using OpenAI-compatible (hosted)
# PROVIDER=OPENAI
# OPENAI_API_KEY=sk-...
# OPENAI_MODEL=gpt-4o-mini
# OPENAI_BASE_URL=https://api.openai.com/v1

# Workspace root (mounted into container at /workspace)
WORKSPACE_ROOT=/workspace

# Safety limits
MAX_FILE_BYTES=1048576  # 1MB file read cap
MAX_SEARCH_BYTES=5242880# 5MB search output cap
MAX_MATCHES=200         # search matches cap